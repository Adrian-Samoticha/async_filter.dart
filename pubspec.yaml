name: async_filter
description: A library for filtering lists, sets, and maps asynchronously.
version: 1.0.0
repository: https://github.com/Adrian-Samoticha/async_filter.dart

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
